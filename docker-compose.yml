services:
  site:
    container_name: site
    build: .
    command: python main.py
    ports:
      - "8000:8000"
    networks:
      - gamejam_network

networks:
  gamejam_network:
    driver: bridge
